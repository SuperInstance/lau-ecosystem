[package]
name = "lau-ecosystem"
version = "0.1.0"
edition = "2021"
description = "Living ecosystem simulation for the Lau game — creatures, food chains, and population dynamics"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
